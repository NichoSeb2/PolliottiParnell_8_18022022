{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form, {'action' : path('task_create')}) }}
        {{ form_widget(form) }}

        <button type="submit" class="btn btn-success">Ajouter</button>
    {{ form_end(form) }}

    <a href="{{ path('task_list') }}" class="btn btn-primary w-auto mt-2">Retour à la liste des tâches</a>
{% endblock %}
